.venv/
.claude/
.env
.env.local
*.env

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Scratch / throwaway scripts (underscore-prefixed) — kept local, never committed
scripts/_*.py

# Eval run artifacts — regenerated on every run (baseline.json IS committed, on purpose)
evals/last_run.json
evals/pr_comment.md
